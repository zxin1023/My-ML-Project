[tool.poetry]
name = "my-ml-project"
version = "0.1.0"
description = "Deep learning research project template"
authors = ["you <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.10"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"